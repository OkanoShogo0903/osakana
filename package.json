{
  "name": "osakana",
  "version": "0.0.1",
  "engines": {
    "node": "12.16.1",
    "npm": "6.14.2"
  },
  "scripts": {
    "heroku-prebuild": "echo This runs before Heroku installs your dependencies. && npm --prefix ./frontend install ./frontend",
    "build":"cd ./frontend && npm run build"

  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
